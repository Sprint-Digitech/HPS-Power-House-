import ClientWrapper from '../ClientWrapper';

export const metadata = {
  title: "HPS Powerhouse",
};

const headContent = ``;
const bodyContent = ``;
const bodyClass = "";

export default function Page() {
  return <ClientWrapper bodyClass={bodyClass} headContent={headContent} bodyContent={bodyContent} />;
}